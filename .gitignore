node_modules
.next
out
dist
.env
.env.local
*.log
.DS_Store
tsconfig.tsbuildinfo

# Prisma SQLite
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm
prisma/dev.db.bak-*
prisma/migrations/dev.db

# Uploads (keep the directory so the Docker build's COPY public ./public
# has something to copy; ignore everything inside it).
public/uploads/*
!public/uploads/.gitkeep

# Docker local-dev volumes
.data/
